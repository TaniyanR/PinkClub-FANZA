# PinkClub サイト修正・最終確認チェックリスト

PinkClub-FANZA を基準サイトとして、今後の修正・完成確認・派生サイトへの反映時に必ず確認する共通チェックリストです。

## 最優先セキュリティ確認

### 公開URLに秘密情報を載せない

公開ページのURL、リンク先URL、リダイレクトURL、canonical URL、OGP URL、RSSリンク、フォームのGET送信先などに、秘密情報・認証情報・個人情報を含めません。

公開URLに載せてはいけない例:

- APIキー / APIシークレット
- パスワード
- セッショントークン / ログイントークン
- CSRFトークン
- パスワード再設定トークン
- 管理用ワンタイムキー
- 秘密の署名値
- メールアドレス、電話番号など公開不要の個人情報
- その他、第三者サイトへRefererとして送信されてはいけない値

商品ID、作品ID、ページ番号、公開検索条件など、第三者に見られても問題のない公開情報のみURLへ使用します。

### `Referrer-Policy: unsafe-url` 採用時の必須条件

PinkClubでは相互リンクの流入元ページを相手サイト側で正確に把握できるよう、公開ページに以下を設定する方針です。

```html
<meta name="referrer" content="unsafe-url">
```

`unsafe-url` では外部サイトへの遷移時に、リンク元URLのパスやクエリ文字列までRefererとして送信される場合があります。

そのため、`unsafe-url` を有効にする前提として、上記「公開URLに秘密情報を載せない」を必須条件とします。

## 修正前の確認

- 修正対象の基準リポジトリを確認する
- PinkClub-FANZA 系は PinkClub-FANZA を基準にする
- API固有部分と共通部分を分ける
- 関係ない機能を変更しない
- 既存DB・既存データを壊さない変更方法を選ぶ
- 公開URLに秘密情報が新たに入らないことを確認する

## 修正後の確認

- PC表示
- スマホ表示
- トップページ
- 商品詳細
- 検索結果
- 女優・ジャンル・メーカー・シリーズ等の商品一覧
- 管理画面
- 相互リンク
- 相互RSS
- SEO / canonical / robots
- OGP / Xカード
- アクセス解析
- エラー処理
- DB migration / 既存DB互換性
- キャッシュ
- リダイレクト
- 公開URLに秘密情報・個人情報・認証情報が含まれていないこと

## 最終完成確認

- 重大・高優先の既知不具合がない
- スマホで横崩れ・極端な文字折返し・操作不能がない
- API固有部分以外の共通機能が基準仕様に揃っている
- 外部リンク・相互リンクで必要なRefererが送られる
- `unsafe-url` 使用時でもURLに秘密情報が存在しない
- ログやエラー画面に秘密情報を不用意に表示しない
- 管理用URLと公開URLの役割が分離されている
- 既存機能にデグレがない

このチェックリストは今後のPinkClub-FANZAの完成確認後、PinkClub-FLおよび各派生サイトへ共通基準として反映します。
